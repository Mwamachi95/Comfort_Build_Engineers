{
  "installCommand": "npm install --include=optional && npm install --os=linux --cpu=x64 sharp",
  "buildCommand": "npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
