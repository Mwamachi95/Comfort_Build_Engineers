{
  "installCommand": "npm install --include=optional --os=linux --cpu=x64",
  "buildCommand": "npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
